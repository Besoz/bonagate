<%= form_for(@user_invitation, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @user_invitation.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@user_invitation.errors.count, "error") %> prohibited this user_invitation from being saved:</h4>

      <ul>
      <% @user_invitation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :reciever_email, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :reciever_email, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :reciever_name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :reciever_name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :company, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :company, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
